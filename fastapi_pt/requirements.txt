# --- web framework & types ---------------------------------
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=1.10     # Pydantic v1 (compatible with transformers)

# --- model & NLP -------------------------------------------
torch>=2.1
transformers>=4.40
sentencepiece       # pulled in by many HF models

# --- optional ONNX / Triton toolchain ----------------------
onnxruntime>=1.17
onnxruntime-tools>=1.13
neural_compressor>=2.5
tritonclient[all]>=2.42
nvidia-pyindex      # to enable pip install of Triton wheels

# --- metrics & monitoring ----------------------------------
prometheus-client>=0.20           # core library
prometheus-fastapi-instrumentator # if you still use it elsewhere
prometheus-flask-exporter         # for any legacy Flask endpoints

# --- async utilities ---------------------------------------
aiofiles>=23.2
httpx>=0.27

# --- evaluation & NLP utils --------------------------------
evaluate>=0.4
nltk>=3.8
rouge-score>=0.1
lime>=0.2

# --- misc ---------------------------------------------------
numpy>=1.26
requests>=2.32
